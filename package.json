{
  "name": "dbms-project",
  "version": "1.0.0",
  "description": "Student Teacher Evaluation DBMS",
  "scripts": {
    "dev": "cd backend && nodemon server.js",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
